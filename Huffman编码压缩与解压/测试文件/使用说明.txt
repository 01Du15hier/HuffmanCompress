1、将源码复制到该目录下
2、在目标文件下打开CMD，输入命令以生成.exe可执行文件：g++ HuffmanCompress.cpp -o HuffmanCompress
3、运行HuffmanCompress.exe
4、若要压缩：
（1）输入1
（2）输入待压缩文件名（扩展名也要写入，若文件与程序不在同一目录，则需给定路径）
（3）输入将要生成的压缩文件名
5、若要解压：
（1）输入2
（2）输入已压缩文件名（扩展名也要写入，若文件与程序不在同一目录，则需给定路径）
（3）输入将要生成的文件名（扩展名也要写入）
6、若要退出，输入3
